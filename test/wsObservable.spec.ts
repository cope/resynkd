import {expect} from 'chai';

import wsObservable from '../src/wsObservable';

describe('wsObservable tests', () => {
	it('wsObservable should exist', () => {
		expect(wsObservable).to.exist;
	});
});
