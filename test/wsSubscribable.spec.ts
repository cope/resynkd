import {expect} from 'chai';

import wsSubscribable from '../src/wsSubscribable';

describe('wsSubscribable tests', () => {
	it('wsSubscribable should exist', () => {
		expect(wsSubscribable).to.exist;
	});
});
